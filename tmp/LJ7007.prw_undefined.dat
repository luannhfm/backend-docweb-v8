#INCLUDE 'PROTHEUS.CH'
#INCLUDE 'RWMAKE.CH'


/*/{Protheus.doc} LJ7007
@description PE para adicionar novos botoes de condicao de pagamento na
			 tela de definicoes de pagto do Venda Assistida.

@author Helitom Silva
@since  03/09/2019

/*/
User Function LJ7007

	Local aRet7007 := ConteudPE()

Return aRet7007


/*/{Protheus.doc} ConteudPE
@description Conteudo do PE tanto para Loja como para Venda Direta.

@author Helitom Silva
@since  03/09/2019

/*/
Static Function ConteudPE

	Local aRet		 := {}

    // IEL (Venda de Certificados)
    If ( cFilAnt = '04MT0001' ) .Or. ( cFilAnt = '03MT0018' )
    		
		aAdd( aRet, 'Transferencia' )
		aAdd( aRet, "Iif(Len(U_SF12A02X(M->LQ_CLIENTE, M->LQ_LOJA)) > 0, LJ7SeleNCC(nOpc), MsgInfo('Este Cliente não possui Creditos no Financeiro','Atenção'))" )	
		
	EndIf
	
Return aRet
