#include 'protheus.ch'
                    
//PE Validação de baixas CP
//Edmar Tinti - Doi Sistemas - 10/03/14
                              
User Function FA080TIT()
Local lRet := .T. 
If Type("lF080Auto") == "L"  .And. lF080Auto
//ExecAuto
ElseIf Type("cMotBx") == "C" 
	If AllTrim(cMotBx) == "NORMAL" .And. Empty(SE2->E2_NUMBCO)
		//If Aviso("BAIXA DE TITULO", "Confirma baixa co motivo NORMAL", {"Sim","Não"}, 1) != 1
		ApMsgStop("Motivo de baixa NORMAL não permitido","FA080TIT")
		lRet := .F.
		//Endif	
	Endif
Endif
Return(lRet)          

