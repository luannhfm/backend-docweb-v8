#INCLUDE 'PROTHEUS.CH'


/*/{Protheus.doc} SF12A01X
@description Avalia alteração do Campo Cod. Cliente Venda Assistida

@author  Helitom Silva
@since   29/08/2019
@version 1.0

@return lRet, Logico, Se codigo do cliente for válido restorna .T.

@type function
/*/
User Function SF12A01X()
	
	Local lRet := .T.
	
	If !Empty(GdFieldGet('LR_PRODUTO'))
		MsgInfo('Após informar produtos, não é permitido alterar o Cliente.', 'Atenção')
		lRet := .F.
	EndIf
	
Return lRet