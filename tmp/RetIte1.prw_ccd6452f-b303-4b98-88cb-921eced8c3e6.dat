#include 'protheus.ch'

/*/{Protheus.doc} retitem
	Função para retornar o Item Contábil para contabilização
@author Microsiga
@return cRet, código do Item Contábil
@history 27/02/2019, Luilton Lucas de Deus, simplificação na lógica
@type user function
/*/
user function RetIte1()
Local _cRet := ""
Local _cItem := SuperGetMV("MV_XITEREN",.F.,"")

If (SubStr(SE1->E1_FILIAL,1,4) $ "02MT/03MT")
	If Empty(SE1->E1_ITEMC)
		_cRet := _cItem
	EndIf
EndIf
	
Return(_cRet)