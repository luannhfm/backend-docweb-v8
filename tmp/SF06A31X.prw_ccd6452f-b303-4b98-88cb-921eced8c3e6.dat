#INCLUDE "TOTVS.CH"

/*/{Protheus.doc} SF06A31X
    @description Fonte utilizado para a tratativa de bloqueio alteração de valor SE1
    @type User Function
    @author Rafael Karczevski
    @since 16/07/2019
    @version 1.0
/*/
User Function SF06A31X()

    Local lBloqVal := GetNewPar("MV_XBALTVL",.T.)
    Local cUserAlt := GetNewPar("MV_XUSALTV","001558,")
    Local lAltera  := Altera //Variavel publica
    Local lRet := .t.

    If lAltera
        If lBloqVal
            If __cUserID $ cUserAlt
                lRet := .t.
            Else
                lRet := .f.
            EndIf
        EndIf
    EndIf

Return lRet