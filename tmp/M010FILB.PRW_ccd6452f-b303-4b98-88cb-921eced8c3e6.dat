#Include 'Protheus.ch'

/*/{Protheus.doc} M010FILB
Ponto de Entrada utilizado para filtrar dados, antes de montar a tela de browse dos produtos. 

@author 	Jose Leite de Barros Neto
@since 	28/07/2015
@version 	1.0

@return 	cFiltro, Deve retornar uma expressão (caracter) válida para o filtro no cadastro de Produtos.

@see 
http://tdn.totvs.com/display/public/mp/M010FILB+-+Cadastro+de+Produtos
/*/
User Function M010FILB()
	
	Local _aArea		:= GetArea()
	Local _xFiltro	
	
	If cModulo $ "COM/EST"
		_xFiltro := "B1_XPRDVEN <> 'S'"
	ElseIf cModulo $ "CRM/FAT"
		_xFiltro := "B1_XPRDVEN <> 'N'"
	Else
		_xFiltro := 1 //Retorna um tipo diferente de caracter, assim o filtro sera desconsiderado
	EndIf
	
	RestArea(_aArea)
	
Return( _xFiltro )