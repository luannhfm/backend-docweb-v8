#INCLUDE 'RWMAKE.CH'
#INCLUDE 'PROTHEUS.CH'


/*/{Protheus.doc} LJ7026
@description PE tem por objetivo filtrar os TiTulos que serão considerados como Credito do Cliente(NCC/RA)

@author  Helitom Silva
@since   30/08/2019
@version 1.0

@return aMakeNCC, Array, Lista contendo os Créditos do Cliente (NCC/RA)

@type function
@see (links_or_references)
/*/
User Function LJ7026

	Local lOnlyView 	:= If(Type("ParamIXB[1]")=="L",ParamIXB[1],.F.) // .T. para que a NCC venha selecionada
	Local aRet			:= {}
	Local aMakeNCC		:= {}

    // IEL (Venda de Certificados)
    If ( cFilAnt = '04MT0001' )
    	aMakeNCC := U_SF12A02X(M->LQ_CLIENTE, M->LQ_LOJA, lOnlyView)
	EndIf
	
	aNCCItens := aMakeNCC

Return aMakeNCC