#include "Protheus.ch"

USER FUNCTION DEVTSX20()
    
    LOCAL OLOG 		:= PARAMIXB[1]
    LOCAL CEMPRESA 	:= PARAMIXB[2]
    	
	SX2 := UPDSX2():CREATE(OLOG, CEMPRESA, "C")
	
	SX2:ADD('ZZZ', "CADASTRO DE TESTE 1"	, "ZM2_FILIAL+ZM2_CODIGO+ZM2_LOJA")
	SX2:ADD('ZZ2', "Cadastro de TESTE 2"	, "ZM3_FILIAL+ZM3_CODIGO")
	
	SX2:CONFIRM()
     
RETURN NIL   
