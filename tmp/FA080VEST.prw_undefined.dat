#include 'protheus.ch'
#include 'parmtype.ch'

/*/{Protheus.doc} FA080VEST
//O ponto de entrada FA080VEST valida as informações 
//antes da montagem da tela de exclusão/cancelamento de baixa.
@author sergio.salustiano
@since 03/02/2020
@version 6

@type function
/*/
user function FA080VEST()

Local dBlqBX :=  SuperGetMv("MV_XBLQFIN",.T.,"20191231") 
Local lRet     := .T.

if(dBlqBX  > DTOS(SE2->E2_BAIXA))
    lRet := .F.
	If MsgYesNo("Este titulo pertence ao periodo fechado da contabilidade, deseja continuar?")
		lRet := .T.
	EndIf

EndIf
	
return lRet
return

	
return