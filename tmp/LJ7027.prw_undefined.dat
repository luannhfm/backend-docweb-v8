	
#INCLUDE 'RWMAKE.CH'

/*/{Protheus.doc} LJ7027
@description Inativar os botões de forma de pagamento desejados da tela de definição de pagamentos (F7), na Venda Assistida

@author Henrique Simões 
@since  24/09/2021

@return lDisableBtn, Logico, Se retornar .T. inativa botão a aparecer entre as formas de Pagamento.

/*/

//Neste exemplo é inativado os botão de pagamento "Cheque" e os botões de pagamento com "Cartão".
User Function LJ7027()
Local lDisableBtn := .F.
Local cFormaPgto := ParamIXB[01]
Local lRet	 := .F.
Local cDesc  := ParamIXB[1]
Local aButFP := {}

    If ( cFilAnt = '03MT0018' ) .and. cFormaPgto == 'DINHEIRO'
            lDisableBtn := .T. //Desabilita botao
    EndIf

    If aScan(aButFP, {|X| X $ Upper(AllTrim(cDesc))}) > 0
		lRet := .T.	
	EndIf

Return lDisableBtn
